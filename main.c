#include <stdio.h>
#include <sys/socket.h>
#include <string.h>
#include <stdlib.h>
#include <ctype.h>

void makeSocket(){
  socket1 = socket(domain, type, protocol);
  return;
}

int main(){
  return 0;
}

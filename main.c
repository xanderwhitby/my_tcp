#include <stdio.h>
#include <sys/socket.h>
#include <string.h> 
#include <stdlib.h>
#include <ctype.h>


int makeSocket(){
  int protocol = 0;
  int socketfd = socket(AF_UNIX, SOCK_STREAM, protocol);
  return socketfd;
}


int main(){
// get the client protocol 
    return 0;


}

                   
